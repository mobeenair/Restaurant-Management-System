prac and prac_log files are SQL Server files database backups
User Controls folder has all the MAIN SCREENS of the application
All the other UI elements/screens are on the main root folder.

In order to run this you have to setup the database by adding the prac files
and also resetting the connection string